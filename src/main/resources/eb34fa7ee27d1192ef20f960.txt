Ви замовили: {%for r in order.cart%}{{r.name}}-{{r.amount}}(={%for k,v in (r.bottles|dictsort)%}{{k}}`*`{{v}}{%if not loop.last%}+{%endif%}{%endfor%})л{%if not loop.last%}; {%endif%}{%endfor%}
Сума до сплати {{(order.sum + order.delivery_fee)|myprintf}} грн.
Форма сплати: {{order.payment}}
Адреса замовлення: {{order.address}}
Телефон: {{order.phone_number}}
