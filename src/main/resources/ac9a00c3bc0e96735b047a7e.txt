{%for c in order.cart-%}
{%if "bottles" in r%}
{{c.amount}} літрів пива "{{c.name}}"
{%else%}
{%for k,v in (c.goods|dictsort|selectattr(1,">",0))%}{{v}} {{k}}{{"; " if not loop.last}}{%endfor%}
{%endif%}
{%endfor-%}
